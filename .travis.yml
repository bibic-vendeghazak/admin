language: node_js
node_js:
- node
before_script:
  - yarn global add firebase-tools
script:
  - yarn build
after_success:
  - firebase use default
  - firebase deploy --only hosting --token "$FIREBASE_TOKEN"