language: node_js
node_js:
- '8'
cache:
  directories:
  - node_modules
install:
  - yarn
script:
  - yarn build
  - yarn test:coverage
# deploy:
#   skip_cleanup: true
#   provider: script
#   script: bash scripts/deploy.sh
#   on:
#     all_branches: true
#     condition: $TRAVIS_BRANCH =~ ^develop|master$
