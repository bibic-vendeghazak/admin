language: node_js
node_js:
- node
before_script:
  - yarn global add firebase-tools
script:
  - yarn build
after_success:
  - curl --ftp-create-dirs -u "$FTP_USERNAME":"$FTP_PASSWORD" -T build ftp://"$FTP_HOST"